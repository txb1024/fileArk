; pnpm 严格模式下默认不会把间接依赖 hoist 到顶层 node_modules,
; 但 @blocknote/core 的子入口 (comments / yjs 等) 直接 import "prosemirror-*",
; Rollup 生产 build 无法 resolve。把 prosemirror 系列 hoist 到顶层即可。
public-hoist-pattern[]=prosemirror-*
