# 2026-05-12

## 項目熟悉 + 全量優化（personal-project-archive）

**項目**：E:/codex — Electron + React + Vite + TypeScript 的 Windows 個人項目資料庫桌面應用。

**完成的優化（共 10 項）**：
1. 工作目錄未設置時顯示警告橫幅，並禁用「新建項目」按鈕
2. 修復深色主題下 violet/orange 的 `--accent-soft` CSS 顏色值錯誤
3. 將 SettingsView 中的 `prompt()`/`confirm()` 替換為 `RenameWorkspaceDialog` / `ConfirmDeleteDialog` 內嵌彈窗組件
4. 收件箱添加單條刪除按鈕、批量刪除按鈕、清空全部按鈕（同步更新 main.cjs / preload.cjs / types.ts）
5. 分類計數邏輯優化：計數為 0 時不顯示，避免誤導
6. 修復 `ProjectsView` 切換項目時 `selectedCategory` / `fileFilter` 不重置的 bug
7. 清理 `styles.css` 中 6 個完全未使用的 CSS class
8. 首頁工作目錄 Metric 添加 `title` tooltip，截斷路徑可 hover 查看完整路徑
9. 添加全局快捷鍵：`Ctrl+K` 聚焦搜索框，`Ctrl+N` 快速新建項目
10. 側邊欄添加項目狀態篩選（全部/進行中/待處理/已歸檔）

**TypeScript 檢查**：`tsc --noEmit` 零錯誤。
